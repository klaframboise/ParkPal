matrix:
    include:
        - language: csharp
          solution: ParkPal.sln
          notifications:
            email: false
          
        - language: node_js
          notifications:
            email: false
          before_script:
            - npm install -g @angular/cli
          script:
            - ng build --prod
